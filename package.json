{
  "name": "testdouble-skillwalker",
  "private": true,
  "packageManager": "bun@1.4.2",
  "workspaces": ["packages/*"],
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:integration": "vitest run --config vitest.integration.config.ts",
    "test:all": "vitest run --config vitest.all.config.ts",
    "lint": "biome lint",
    "format:check": "biome format",
    "check": "biome check",
    "typecheck": "for pkg in packages/bun-helpers packages/claude-integration packages/cli packages/data packages/sandbox-integration packages/evals packages/execution packages/web; do (cd $pkg && bunx tsc --noEmit) || exit 1; done",
    "ci-checks": "npm run lint && npm run format:check && npm run typecheck && npm run test && npm run test:integration && bun audit --audit-level=moderate"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.14",
    "typescript": "^7.0.2",
    "vitest": "^5.0.1"
  },
  "overrides": {
    "vite": "^8.3.0",
    "picomatch": "^4.0.7",
    "hono": "^4.13.8",
    "postcss": "^8.5.23"
  }
}
